<!DOCTYPE html>
<html>
  <head>
    <title>HadithDaily - Dashboard</title>
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.7/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <link rel="icon" href="favicon/favicon.ico" type="image/x-icon" />
    <link rel="icon" href="favicon/favicon-16x16.png" sizes="16x16" />
    <link rel="icon" href="favicon/favicon-32x32.png" sizes="32x32" />
    <style>
      .container {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        padding: 2rem;
      }
      .dashboard-card {
        background-color: #f3f4f6;
        border-radius: 8px;
        padding: 1.5rem;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }
      .dashboard-title {
        font-size: 24px;
        font-weight: bold;
        color: #1f2937;
        margin-bottom: 1rem;
      }
      .quote {
        font-size: 18px;
        color: #374151;
        margin-bottom: 1rem;
      }
      .quote-reference {
        font-size: 14px;
        color: #6b7280;
      }
      .islamic-greeting {
        font-size: 20px;
        font-weight: bold;
        color: #1f2937;
        margin-bottom: 1rem;
      }
      .text-3d {
        text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        transform: perspective(200px) rotateX(20deg);
      }
    </style>
  </head>
  <body class="bg-gray-100">
    <div class="container">
      <div class="dashboard-card">
        <h1 class="islamic-greeting">As-salamu alaykum (Peace be upon you)</h1>
        <h1 class="dashboard-title">
          Welcome to the
          <span
            class="font-bold text-gradient bg-gradient-to-r from-green-400 to-cyan-500 text-3d"
            >HadithDaily</span
          >
        </h1>
        <div class="quote">"<%= quote %>"</div>
        <div class="quote-reference">- Reference: <%= reference %></div>
      </div>
    </div>
  </body>
</html>
